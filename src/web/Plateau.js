/**
 * Created by bcleton on 08/01/18.
 */
Breakthrough.Plateau = function(c) {
        var container = c;
        var board;

        this.initialize = function() {
            board = new Array();
            for(var i = 0; i < 8; i++)
            {
                board[i] = new Array();
                for(var j = 0; j < 8; j++)
                {

                    board[i][j] =
                }
            }
        };

};